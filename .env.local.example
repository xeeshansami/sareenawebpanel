# Copy to .env.local to develop against a backend running on this machine.
#
# Without it, `npm run dev` uses .env, which points at the deployed API — so a
# route added locally but not yet deployed answers "Route ... not found".
VITE_API_BASE_URL=http://localhost:3000/api/v1
